---
name: cover-face-title
display_name: 换脸干货封面
description: 为小红书等新媒体平台生成干货教程风封面图。用户提供人脸参考图替换封面中的人物形象，自行填写主标题和副标题文案，并可用文字描述背景主题；默认输出新媒体干货感手账拼贴风（贴纸描边人物、亮绿黑边卡通黑体标题、贴纸装饰、居中包围构图），也可按用户要求切换其他风格。适用于账号封面、教程干货封面、种草封面等图片创作场景。
tools: ["sandbox_generate_image"]
---

# 换脸干货封面

## 1. 核心原则

封面是新媒体内容的「门面钩子」：一张合格的干货封面必须在 0.5 秒内同时传递「这是谁」「讲什么」「为什么值得点」。本技能用三要素锁定这一点：**一张让人记住的脸（贴纸化人物）+ 一句说人话的标题（加粗卡通黑体）+ 一个与主题强相关的场景暗示（背景）**。三者缺一，封面就退化为普通配图。

持久引擎（因果链）：
**人脸参考 → 贴纸化剥离背景（记忆点）→ 主标题承诺利益（点开理由）→ 副标题降低门槛（如「新手直接抄作业」）→ 背景呼应主题（可信度）→ 贴纸装饰补网感（停留时长）→ 点击与关注转化**

## 2. 质量测试（可证伪）

把成图缩小到拇指大小看：**人脸轮廓清晰可辨、主标题完整可读、副标题至少能认出一半**。三者任一不满足即判定失败，调整 prompt（放大标题字号、加强描边、简化背景层次）后重做，不得直接交付。

## 3. 输入诊断（落 prompt 前必须确认）

必需输入，缺哪项就只追问哪项，不要一次追问全部：
- **人脸参考图**（1 张）：用户上传的人物照片，用于替换封面人物的脸。
  - 合规前提：仅使用用户本人提供、且已确认拥有肖像使用授权的照片；若用户上传他人照片，先提示确认授权，未确认不得继续。
- **主标题文案**：封面顶部与底部的核心标题（用户填写，通常上下对称重复或呼应）。
- **副标题文案**：侧边引导语（用户填写，如「新手运营 直接抄作业」）。
- **背景主题描述**：用户给出的背景文字（如「教别人做自媒体」→ 笔记本电脑+数据图表；「减肥餐教程」→ 厨房+食材）。用户未给时，从主标题语义推导并默认使用「露出数据图表的笔记本电脑室内场景」。

可选项：
- **画幅**：默认 3:4（小红书封面），用户可指定其他比例。
- **人物姿态/表情**：默认「皱眉认真表情、手指向前指引」，用户可指定。
- **风格**：默认新媒体干货手账拼贴风；用户点名其他风格时整体切换（见路由表）。

## 4. 路由表

| 输入特征 | 选用路线 | 必读 reference |
|---|---|---|
| 未指定风格（默认） | 干货手账拼贴风 | references/formats/collage-style.md |
| 用户点名其他风格（如极简风、3D 风、纯文字封面） | 按用户描述执行，仅保留「人脸替换 + 双标题」结构 | 无需额外加载 |

## 5. 强制加载 reference

默认风格路线在落 prompt 前**必须**读取 `references/formats/collage-style.md`，把其中的风格词库与构图规范写进实际生成描述；不得凭记忆概括。切换风格路线时可跳过。

## 6. 工作流

1. **收集输入**：按「输入诊断」确认四项必需信息；素材缺失只追问缺失项，一次最多补问一轮。
2. **组装生成描述**：
   - 结构 = 风格基调（来自 collage-style.md）+ 画幅 + 三层构图（背景层 / 人物层 / 文字贴纸层）+ 精确文案（主标题、副标题逐字写入，不得改写用户文案）+ 装饰贴纸建议（呼应背景主题）。
   - 人脸一致性：将用户上传的人脸参考图作为图片参考传入，并在描述中明确「人物面部特征与参考图保持一致」。
   - 用户描述背景时，把文字转译为具体可画的中景场景描述，再嵌入背景层。
3. **生成**：每次产出 1 张封面图；不通过质量测试则静默调整重做，**最多重做 2 次**。通过后交付，并说明可继续微调的方向（标题字号、贴纸元素、背景密度）；仍不通过则交付当前最优稿，并如实说明未达标项与人工调整建议，不得无上限反复重试。

## 7. 输出契约

生成描述必须包含固定字段，按此模板组装：

```
[画幅：默认 3:4]
[风格基调：新媒体干货手账拼贴风——整体主色调、背景虚化程度]
[背景层：依据用户背景文字转译的具体场景，中景、轻微虚化]
[人物层：以参考图人脸生成的人物，白色粗描边贴纸化处理剥离背景，姿态/表情，置于画面中央视觉重心]
[文字层：顶部与底部加粗亮绿黑边卡通黑体主标题「{主标题}」，侧边撞色粗体副标题「{副标题}」]
[装饰层：与主题呼应的卡通贴纸 2-3 个（如钱袋、AI 气泡、数据节点），不遮挡人脸与标题]
[构图：居中对称，上下标题形成隐形包围式边框，层次从背景到人物到文字逐级递进]
```

文案规则：主标题与副标题**逐字使用用户提供的文字**，不增删改写；标题措辞用正向表达，不堆否定式约束。

## 8. 静默自检（出稿前，不外显）

- 人脸是否与参考图明显同一人？
- 主标题、副标题文字是否与用户填写内容逐字一致、无错字？
- 拇指测试：缩小后主标题是否可读、人脸是否可辨？
- 背景是否与主题相关——用户给了背景描述时按其转译；用户未给时，是否由主标题语义推导而来，而非无脑套用兜底场景？
- 装饰贴纸是否克制（2-3 个）且不遮挡人脸与标题？
- 画幅是否符合用户指定（默认 3:4）？
- 交付时是否附带了微调建议？
